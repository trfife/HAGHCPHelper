{
  "config": {
    "step": {
      "user": {
        "title": "Choose Backend",
        "description": "Select which AI backend to use for conversation.",
        "data": {
          "backend": "Backend"
        }
      },
      "github": {
        "title": "GitHub Models",
        "description": "Enter your GitHub Personal Access Token with the `models:read` permission.",
        "data": {
          "github_token": "GitHub Token (PAT)",
          "model": "Model"
        },
        "data_description": {
          "github_token": "A fine-grained PAT with `models:read` scope. Create one at github.com/settings/personal-access-tokens/new",
          "model": "Select a model or type a custom model ID (format: publisher/model-name)"
        }
      },
      "azure": {
        "title": "Azure AI Endpoint",
        "description": "Enter your Azure AI endpoint URL and API key.",
        "data": {
          "azure_endpoint": "Endpoint URL",
          "azure_api_key": "API Key",
          "model": "Model Name"
        },
        "data_description": {
          "azure_endpoint": "The full URL of your Azure AI chat completions endpoint",
          "azure_api_key": "Your Azure AI API key",
          "model": "The deployed model name (e.g., gpt-4o)"
        }
      }
    },
    "error": {
      "invalid_auth": "Authentication failed. Check your token or API key.",
      "cannot_connect": "Could not connect to the API. Check the endpoint and try again.",
      "unknown": "An unexpected error occurred."
    },
    "abort": {
      "already_configured": "This integration is already configured."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Options",
        "description": "Update your API credentials or model selection.",
        "data": {
          "github_token": "GitHub Token (PAT)",
          "model": "Model",
          "azure_endpoint": "Endpoint URL",
          "azure_api_key": "API Key"
        }
      }
    }
  },
  "config_subentries": {
    "conversation": {
      "step": {
        "user": {
          "title": "New Conversation Agent",
          "description": "Create a new conversation agent with custom settings.",
          "data": {
            "title": "Name",
            "llm_hass_api": "Home Assistant APIs",
            "prompt": "System Prompt",
            "temperature": "Temperature",
            "max_tokens": "Max Tokens"
          },
          "data_description": {
            "title": "Display name for this conversation agent",
            "llm_hass_api": "Which HA APIs should this agent have access to (e.g., Assist for entity control)",
            "prompt": "Custom system prompt to override the default. Leave empty for the default prompt.",
            "temperature": "Controls randomness (0 = deterministic, 1 = creative)",
            "max_tokens": "Maximum number of tokens in the response"
          }
        }
      }
    }
  }
}
